#ifndef _BVGZ_CONFIG_H
#define _BVGZ_CONFIG_H

// allocation size of function stack
#define FUNC_STACK_ALLOC_SZ     64

// child processes structures allocation step
#define PROC_ALLOC    8

// networking: backlog used with listen()
#define LISTEN_BACKLOG  10

// i/o event structures allocation step
#define IO_EVT_ALLOC    4

// sleep duration of idle steps (milliseconds)
#define IDLE_STEP_DURATION_MS       10

// number of instructions every which a cleanup is done
#define VM_CLEANUP_PERIOD_INSTNS    20

#undef ENABLE_INSTN_PRINT
// #define ENABLE_INSTN_PRINT

#endif
